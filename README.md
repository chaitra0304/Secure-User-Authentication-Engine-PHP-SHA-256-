# Secure-User-Authentication-Engine-PHP-SHA-256-
A secure authentication system built with PHP and a CSV backend that uses SHA-256 hashing to protect user passwords. Credentials are verified by hashing login inputs and matching them with stored hashes. The system includes access control and prevents server-side script information leakage.
